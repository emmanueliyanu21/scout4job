const initState = {
    vitaes: [
        {
            'firstName': 'Emmanuel',
            'lastName': 'Oladejo',
        }
    ]
}

const vitaeReducer = (state = initState, action) => {
    return state
}

export default vitaeReducer