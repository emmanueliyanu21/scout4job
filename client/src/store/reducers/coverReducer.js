const initState = {
    covers: [
        {
            'firstName': 'Emmanuel',
            'lastName': 'Oladejo',
        }
    ]
}

const coverReducer = (state = initState, action) => {
    return state
}

export default coverReducer